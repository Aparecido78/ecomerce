<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mercado Black Friday</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #000, #555);
            color: #fff;
        }

        .container {
            text-align: center;
            width: 90%;
            max-width: 600px;
        }

        h1.title {
            font-size: 3rem;
            margin-bottom: 50px;
            font-weight: 600;
            letter-spacing: 2px;
        }

        .login-options {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .login-card {
            background: rgba(0,0,0,0.8);
            padding: 30px;
            border-radius: 20px;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .login-card:hover {
            background: rgba(255,255,255,0.1);
            box-shadow: 0 0 25px #fff;
            transform: translateY(-5px);
        }

        .login-card a {
            text-decoration: none;
            color: #fff;
            font-size: 1.8rem;
            font-weight: 600;
        }

        @media (max-width: 500px) {
            h1.title {
                font-size: 2rem;
                margin-bottom: 30px;
            }
            .login-card a {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">Mercado <br><span style="color:#FFD700;">Black Friday</span></h1>
        <div class="login-options">
            <div class="login-card">
                <a href="/login/cliente">Login Cliente</a>
            </div>
            <div class="login-card">
                <a href="/login/vendedor">Login Vendedor</a>
            </div>
        </div>
    </div>
</body>
</html>
