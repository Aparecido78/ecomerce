<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Produto</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<a href="/tela/produtos"
   style="position: fixed;
          top: 20px;
          left: 20px;
          display: inline-block;
          padding: 10px 15px;
          background: greenyellow;
          color: white;
          text-align: center;
          border-radius: 5px;
          text-decoration: none;
          font-weight: bold;
          z-index: 1000;">
   &lt; Voltar
</a>

<body class="bg-light d-flex justify-content-center align-items-center" style="height:100vh;">

   <% if (mensagem) { %>
    <p style="color: green; margin-left:60%; font-size:30px;"><%= mensagem %></p>
    
<% } %>

  <div class="card bg-white text-dark border-dark p-3 shadow" style="width: 360px;">
    <h4 class="text-center mb-3">Editar Produto</h4>
    
    <form action="/editar/produto/vendedor/<%= produtos.id %>" method="post" enctype="multipart/form-data">
        
      <div class="mb-2">
        <label class="form-label fw-bold small">Nome</label>
        <input type="text" name="nome" class="form-control form-control-sm bg-dark text-white border-0" value="<%= produtos.nome %>">
      </div>

      <div class="mb-2">
        <label class="form-label fw-bold small">Preço</label>
        <input type="number" name="preco" class="form-control form-control-sm bg-dark text-white border-0" value="<%= produtos.preco %>">
      </div>

      <div class="mb-2">
        <label class="form-label fw-bold small">Quantidade</label>
        <input type="number" name="quantidade" class="form-control form-control-sm bg-dark text-white border-0" value="<%= produtos.quantidade %>">
      </div>

      <div class="mb-2">
        <label class="form-label fw-bold small">Descrição</label>
        <textarea name="descricao" class="form-control form-control-sm bg-dark text-white border-0" rows="4"><%= produtos.descricao %></textarea>
      </div>

      <div class="mb-2">
        <label class="form-label fw-bold small">Foto do Produto</label>
        <% if (produtos.foto) { %>
          <div class="mb-1">
            <img src="/image/<%= produtos.foto %>" class="img-fluid" style="max-height: 120px;" alt="Foto atual">
          </div>
        <% } %>
        <input type="file" name="foto" class="form-control form-control-sm">
      </div>

      <button type="submit" class="btn btn-dark btn-sm w-100">Salvar alterações</button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
